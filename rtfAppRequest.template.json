{
    "type": "RF",
    "name": "{{RTFAppName}}",
    "target": {
        "targetId": "{{targetId}}",
        "deploymentSettings": {
            "runtimeVersion": "{{runtimeVersion}}"
        }
    },
    "expectedStatus": "deployed"
}